name: Master and Master PR test

on:
  pull_request:
    branches: new-branch
  push:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Hello
        run: echo "Hello!!!"
